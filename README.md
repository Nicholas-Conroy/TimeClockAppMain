# TimeClock App
Mobile App Class project using React Native that allows the user to sign up/log in to the app, clock in and out of shifts and see shift history. A simple Firebase database is used to store user data, clock in/out times, etc.
